#include<bits/stdc++.h> 
using namespace std ; 

int main() {
/*
	long a, b ; 
	char s; 

	cin >> a >> s >> b;


	if (s == '+') {cout << a + b;}

	if (s == '-') {cout << a - b;}

	if (s == '*') {cout << a * b;}

	if (s == '/') {cout << a / b;}
  */ 
  int n = 15; 
  for (int i = 0; i <= n; ++i){
    cout << i << endl; 
  }

  for (int i = 0; i <= n; i++){
    cout << i << endl;
  }
  return 0; 
}
