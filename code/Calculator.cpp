#include<bits/stdc++.h> 
using namespace std ; 

int main() {

	long a, b ; 
	char s; 

	cin >> a >> s >> b;


	if (s == '+') {cout << a + b;}

	if (s == '-') {cout << a - b;}

	if (s == '*') {cout << a * b;}

	if (s == '/') {cout << a / b;}



}